<Window x:Class="Medicom.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:views="clr-namespace:Medicom.Views"
        Title="{Binding Title}" Height="350" Width="525">
    <Grid>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Сохранить" IsEnabled="{Binding IsSaveFile}" Command="{Binding SaveFileCommand}" />
                <Separator />
                <MenuItem Header="Выйти" Command="{Binding ExitCommand}" />
            </MenuItem>
        </Menu>
        <!--<ContentControl prism:RegionManager.RegionName="ContentRegion" />-->
        <ContentControl>
            <views:ItemsView />
        </ContentControl>
    </Grid>
</Window>
